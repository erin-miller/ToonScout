{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- packages/webapp || exit 1; fi"
}
