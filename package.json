{
  "private": true,
  "workspaces": [
    "packages/api",
    "packages/bot",
    "packages/webapp"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:api": "vitest run packages/api",
    "test:webapp": "vitest run packages/webapp",
    "test:scripts": "echo 'Add standalone test scripts to scripts/tests/'"
  },
  "devDependencies": {
    "vitest": "^4.0.17"
  }
}
